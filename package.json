{
  "name": "pokernow-copilot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vite build && cross-env BUILD_TARGET=content vite build",
    "dev": "concurrently \"vite build --watch\" \"cross-env BUILD_TARGET=content vite build --watch\"",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "concurrently": "^10.0.0",
    "cross-env": "^10.1.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
